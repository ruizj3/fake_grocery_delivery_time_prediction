[project]
name = "delivery-ml"
version = "0.1.0"
description = "ML pipeline for delivery time prediction"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/delivery_ml"]
